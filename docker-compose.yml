version: '3.8'

services:
  landing-lora:
    build: .
    container_name: landing-lora-react
    restart: unless-stopped
    ports:
      - "3000:80"
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost:3000"]
      interval: 30s
      timeout: 10s
      retries: 3
    networks:
      - coolify
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=coolify"
      - "traefik.http.routers.landing-lora.rule=Host(`lora.alystechpy.online`)"
      - "traefik.http.routers.landing-lora.entrypoints=https"
      - "traefik.http.routers.landing-lora.tls.certresolver=letsencrypt"
      - "traefik.http.services.landing-lora.loadbalancer.server.port=80"

networks:
  coolify:
    external: true